export const LIBS_VERISONS_TOML_PATH = "libs.version.toml";

export const TOML_SPLITTERS = {
  DEPS: "#END_DEPS",
};

export const PACKAGES = ["roomdb"];
export const PACKAGES_NAMES = {
  ROOMDB: "roomdb",
};
